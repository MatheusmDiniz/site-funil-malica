<header class="header">
  <div class="container header__inner">
    <a href="/" class="header__logo" aria-label="Malica — página inicial">
      <img
        src="/images/malica-logo.png"
        alt="Malica"
        width="140"
        height="40"
        fetchpriority="high"
      />
    </a>
  </div>
</header>

<style>
  .header {
    padding-block: 0.55rem 0.25rem;
  }

  .header__inner {
    display: flex;
    justify-content: center;
  }

  .header__logo {
    display: inline-flex;
    text-decoration: none;
  }

  .header__logo:focus-visible {
    outline: 2px solid var(--malica-verde-salvia);
    outline-offset: 3px;
    border-radius: 4px;
  }

  .header__logo img {
    width: auto;
    height: 2.5rem; /* ~40px — marca legível no mobile */
    object-fit: contain;
  }

  @media (min-width: 640px) {
    .header__logo img {
      height: 2.85rem;
    }
  }
</style>
